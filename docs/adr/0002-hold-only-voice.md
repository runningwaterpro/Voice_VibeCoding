# Voice interaction is hold-only

The supported voice interaction is press-to-talk hold: press begins the interaction and release ends it. Toggle and hands-free options are not supported in this scope, even if legacy configuration fields remain readable for migration; exposing them as active choices would make the product promise behavior it does not implement.
